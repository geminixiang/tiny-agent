{
  "name": "tiny-agent-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "tiny-ts": "./dist/src/cli.js" },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc",
    "prepare": "npm run build",
    "check": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.23.0",
    "typescript": "^7.0.0"
  }
}
